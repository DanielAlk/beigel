<% if notice.present? or alert.present? %>
	<% modal_class = alert.present? ? 'modal-danger' : 'modal-primary' %>
	<% content_for :extra_js do %>
		Modals.notice();
	<% end %>
	
  <div class="modal fade <%= modal_class %>" tabindex="-1" role="dialog" id="modalNotice">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h3 class="modal-title">Beigel Bienes Raices</h3>
        </div>
        <div class="modal-body">
          <p>
            <%= notice.html_safe if notice.present? %>
            <%= alert.html_safe if alert.present? %>
          </p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-primary" data-dismiss="modal">OK</button>
        </div>
      </div>
    </div>
  </div>
<% end %>